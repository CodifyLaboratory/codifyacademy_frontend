version: '3.5'
services:
  frontend:
    build: .
    restart: always
    environment:
      - VIRTUAL_HOST=holoclinic.io
      - VIRTUAL_PORT=3001
    networks:
      mypupio: {}
networks:
  mypupio:
    name: mypupio
